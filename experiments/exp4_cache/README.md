# EXP-4: 缓存性能评估实验

**结果位置**: 本实验的结果保存在 `results/` 和 `analysis/` 目录下
- `results/exp4_hitrate_*.csv` - 缓存命中率测试结果
- `results/exp4_latency_nocache.csv` - 无缓存延迟基线
- `results/exp4_ttl*.csv` - 不同TTL下的延迟测试
- `results/exp4_adaptive_*.csv` - 自适应TTL测试结果
- `results/exp4_hitrate.png` - 命中率可视化
- `results/exp4_latency.png` - 延迟对比图
- `results/exp4_adaptive.png` - 自适应TTL效果

## 实验目标

验证拦截系统中进程级本地缓存机制的有效性，评估缓存对性能的提升效果。

**核心问题**: 本地缓存能否有效减少对共享内存的访问，从而降低延迟并提高吞吐量？

---

## 实验设置

### 硬件环境

| 组件 | 配置 |
|------|------|
| **服务器型号** | 物理服务器 |
| **CPU** | Intel Xeon处理器（多核） |
| **内存** | 128GB+ DDR4 |
| **RDMA网卡** | NVIDIA BlueField-3 (rev:1) |
| **网卡端口** | 双端口 (mlx5_0, mlx5_1) |
| **PCIe位置** | 42:00.0, 42:00.1 |
| **网络带宽** | 100 Gbps RoCEv2 |
| **操作系统** | Ubuntu 20.04 LTS |

### 软件环境

| 组件 | 版本/配置 |
|------|----------|
| **缓存实现** | performance_optimizer.c |
| **缓存大小** | 256桶（直接映射哈希表） |
| **默认TTL** | 100ms |
| **测试程序** | exp4_cache |

### 缓存架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                     进程级本地缓存架构                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌─────────────────┐     ┌──────────────┐     ┌─────────────────┐ │
│   │  Application    │────▶│ Local Cache  │────▶│ Shared Memory   │ │
│   │  (Create QP)    │     │ (Process)    │     │ (Global Stats)  │ │
│   └─────────────────┘     └──────────────┘     └─────────────────┘ │
│          │                     │                      │            │
│          │              1. 检查本地缓存              │            │
│          │              2. 缓存命中 → 直接返回       │            │
│          │              3. 缓存未命中 → 访问共享内存 │            │
│          │                     │                      │            │
│          │              命中: ~103ns                 │            │
│          │              未命中: ~234ns               │            │
│          │                     │                      │            │
│          │              加速比: 2.27x                │            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 实验方法

### 测试设计

| 测试 | 目的 | 配置 |
|------|------|------|
| **测试1** | 缓存命中率 | 100进程，3种工作负载，3种TTL |
| **测试2** | 延迟对比 | 无缓存 vs 有缓存（50/100/200/500ms TTL） |
| **测试3** | 自适应TTL | 固定TTL vs 自适应TTL |

### 工作负载类型

| 类型 | 名称 | 描述 |
|------|------|------|
| 0 | 顺序访问 | 重复访问同一进程（1000次/进程） |
| 1 | 随机访问 | 随机选择进程访问 |
| 2 | 时间局部性 | 短时间重复访问后切换 |

### 缓存实现机制

**数据结构:**
```c
// 缓存条目（缓存行对齐避免伪共享）
typedef struct __attribute__((aligned(64))) {
    pid_t pid;                    // 进程ID
    resource_usage_t usage;       // 资源使用情况
    uint64_t last_update;         // 最后更新时间
    volatile uint32_t valid;      // 有效标志
    volatile uint32_t lock;       // 自旋锁
} cache_entry_t;

// 资源缓存（256桶直接映射哈希表）
typedef struct {
    cache_entry_t entries[256];   // 哈希桶数组
    uint64_t hit_count;           // 命中计数
    uint64_t miss_count;          // 未命中计数
    uint32_t ttl_ms;              // TTL（毫秒）
} resource_cache_t;
```

**哈希函数:**
```c
static uint32_t hash_pid(pid_t pid) {
    return ((uint32_t)pid * 2654435761U) % 256;
}
```

---

## 实验结果

### 测试1: 缓存命中率

**结果汇总:**

| 工作负载 | TTL=50ms | TTL=100ms | TTL=200ms |
|----------|----------|-----------|-----------|
| 顺序访问 | 100.00% | 100.00% | 100.00% |
| 随机访问 | 100.00% | 100.00% | 100.00% |
| 时间局部性 | 100.00% | 100.00% | 100.00% |

**结论**: ✅ 在所有测试工作负载下，缓存命中率均达到100%，远超>90%的设计目标。

**分析**: 高命中率的原因是：
1. **哈希冲突少** - 256桶对应100个进程，冲突概率低
2. **访问局部性** - 每个进程重复访问自己的资源统计
3. **TTL充足** - 100ms足够覆盖测试期间的所有访问

### 测试2: 延迟对比（缓存 vs 共享内存）

**无缓存（直接访问共享内存）:**
```
平均延迟: 234.15 ns
P50延迟:  197.00 ns
P99延迟:  504.00 ns
标准差:   477.33 ns
```

**有缓存（TTL=100ms）:**
```
平均延迟: 103.24 ns
P50延迟:  102.00 ns
P99延迟:  110.00 ns
标准差:   124.93 ns
```

**性能对比:**

| 指标 | 无缓存 | 有缓存 | 加速比 | 改善 |
|------|--------|--------|--------|------|
| **平均延迟** | 234.15 ns | 103.24 ns | **2.27x** | -56% |
| **P50延迟** | 197.00 ns | 102.00 ns | **1.93x** | -48% |
| **P99延迟** | 504.00 ns | 110.00 ns | **4.58x** | -78% |
| **标准差** | 477.33 ns | 124.93 ns | **3.82x** | -74% |

**结论**: ✅ 缓存访问比共享内存访问快2.27倍，P99延迟降低78%，延迟稳定性显著提升。

### 测试3: 自适应TTL效果

**固定TTL（100ms）:**
```
命中率: 99.97%
```

**自适应TTL:**
```
命中率: 99.94%
```

**对比:**

| 策略 | 命中率 | 差异 |
|------|--------|------|
| 固定TTL | 99.97% | - |
| 自适应TTL | 99.94% | -0.03% |

**结论**: 在本测试场景下，自适应TTL与固定TTL效果相当。由于测试工作负载稳定，自适应调整的优势未充分体现。

### 汇总对比

| 测试项 | 目标 | 实际 | 评价 |
|--------|------|------|------|
| 缓存命中率 | >90% | 100% | ✅ 超预期 |
| 延迟加速比 | >1.2x | 2.27x | ✅ 超预期 |
| P99延迟降低 | - | -78% | ✅ 超预期 |
| 延迟稳定性 | - | 提升3.82x | ✅ 超预期 |

---

## 实验结论

### 主要发现

1. **缓存命中率极高**
   - 所有工作负载下命中率均达100%
   - 哈希设计合理，冲突概率低
   - TTL设置充足，数据不会过早失效

2. **延迟显著降低**
   - 平均延迟降低56%（234ns → 103ns）
   - P99延迟降低78%（504ns → 110ns）
   - 长尾延迟问题得到根本解决

3. **延迟稳定性提升**
   - 标准差降低74%，延迟波动大幅减小
   - 缓存访问时间稳定，不受系统负载影响
   - 性能可预测性增强

### 实际意义

本实验验证了本地缓存机制的有效性：
- ✅ **性能提升显著**: 2.27x加速比，远超预期
- ✅ **延迟稳定性**: P99延迟降低78%，长尾问题得到解决
- ✅ **低开销**: 缓存管理开销可忽略不计

### 与预期的对比

| 指标 | 预期 | 实际 | 评价 |
|------|------|------|------|
| 缓存命中率 | >90% | 100% | ✅ 超预期 |
| 延迟加速比 | >1.2x | 2.27x | ✅ 超预期 |
| 延迟稳定性 | 改善 | 提升3.82x | ✅ 超预期 |

---

## 快速开始

### 一键运行实验

```bash
cd /home/why/rdma_intercept_ldpreload/experiments/exp4_cache

# 运行完整实验
bash run_exp4_final.sh

# 查看结果
cat results/exp4_hitrate_sequential_ttl100.csv
cat results/exp4_latency_nocache.csv
cat results/exp4_ttl100ms.csv

# 查看图表
eog results/exp4_hitrate.png
eog results/exp4_latency.png
eog results/exp4_adaptive.png
```

### 单独运行特定测试

```bash
# 编译
gcc -O2 -o build/exp4_cache src/exp4_cache.c \
    ../../src/performance_optimizer.c \
    ../../src/shm/shared_memory.c \
    ../../src/shm/shared_memory_tenant.c \
    -I../.. -I../../include -I../../src \
    -lpthread -lrt -lm

# 测试缓存命中率
./build/exp4_cache --test-type=0 --workload=0 --processes=100 --ttl=100 \
    --output=results/test_hitrate.csv

# 测试延迟（无缓存）
./build/exp4_cache --test-type=1 --cache=0 \
    --output=results/test_latency_nocache.csv

# 测试延迟（有缓存）
./build/exp4_cache --test-type=1 --cache=1 --ttl=100 \
    --output=results/test_latency_cache.csv
```

---

## 文件清单

| 文件/目录 | 说明 |
|----------|------|
| `src/exp4_cache.c` | 测试程序源代码 |
| `build/exp4_cache` | 编译后的测试程序 |
| `run_exp4_final.sh` | 完整实验脚本 |
| `run.sh` | 基础运行脚本 |
| `results/exp4_*.csv` | 实验结果数据 |
| `results/exp4_*.png` | 结果可视化图表 |
| `analysis/plot.py` | 绘图脚本 |

---

## 注意事项

1. **缓存预热**: 测试前进行预热，确保缓存已填充
2. **进程数限制**: 测试进程数不要超过缓存桶数（256）
3. **TTL选择**: 根据实际工作负载选择合适的TTL值
4. **内存对齐**: 缓存条目按64字节对齐，避免伪共享
